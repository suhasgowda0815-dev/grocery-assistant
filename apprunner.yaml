version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip3 install -r requirements.txt
run:
  command: python3 app.py
  network:
    port: 8080
  env:
    - name: AWS_DEFAULT_REGION
      value: ap-south-1
```

---

### **File 3: `requirements.txt`** (in root)
```
Flask==3.0.0
boto3==1.34.0
Werkzeug==3.0.1
